<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:background="@color/page_background"
    tools:context="bbabics.hu.newsreader.activities.MainActivity">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ListView
            android:id="@+id/fragment_main_listView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:background="@color/listview_background"
            android:divider="@color/listview_divider"
            android:dividerHeight="@dimen/divider_height" />
    </LinearLayout>

    <com.melnykov.fab.FloatingActionButton
        android:id="@+id/fragment_main_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="@dimen/main_fab_margin"
        android:src="@drawable/fab_icon"
        fab:fab_colorNormal="@color/fab_primary"
        fab:fab_colorPressed="@color/fab_primary_dark" />
</FrameLayout>
